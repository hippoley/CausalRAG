name: Deploy verified A/B replay

on:
  push:
    branches: [main]
    paths:
      - 'branchpoint/**'
      - 'examples/**'
      - 'site/**'
      - 'scripts/build_pages.py'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install runtime
        run: pip install -e '.[dev]'
      - name: Verify browser workbench syntax
        run: node --check site/app.js
      - name: Verify runtime and build static replay
        run: |
          pytest -q tests/test_experiment_contracts.py tests/test_hidden_world_stochastic.py tests/test_playable_probe_compare.py
          python scripts/build_pages.py
          python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path("dist/data/runtime.json").read_text(encoding="utf-8"))
          report = data["playable_ab"]
          assert report["comparison"]["same_world_inputs"] is True
          assert report["first_divergence"] is not None
          assert report["first_divergence"]["step"] == 0
          assert report["vanilla"]["metrics"]["success"] is False
          assert report["causal"]["metrics"]["success"] is True
          assert report["vanilla"]["decisions"][0]["selected"]["name"] == "submit_form"
          assert report["causal"]["decisions"][0]["selected"]["name"] == "inspect_submission_state"
          examples = data["examples"]
          assert set(examples) == {"route", "incident", "browser"}
          assert all(run["episode_ledger"] for run in examples.values())
          assert examples["route"]["metrics"]["success"] is True
          assert examples["route"]["decisions"][0]["selected"]["name"] == "inspect_request_shape"
          assert examples["incident"]["metrics"]["success"] is True
          assert examples["incident"]["decisions"][0]["selected"]["name"] == "read_latency_signature"
          assert examples["browser"]["metrics"]["success"] is True
          assert examples["browser"]["decisions"][0]["selected"]["name"] == "inspect_submission_state"
          PY
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
