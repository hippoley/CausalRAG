services:
  - type: web
    name: causalrag-playable-probe
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: BRANCHPOINT_API_HOST
        value: 0.0.0.0
      - key: BRANCHPOINT_API_PORT
        value: 8765
      - key: BRANCHPOINT_REQUIRE_PROBE_AUTH
        value: "true"
      - key: BRANCHPOINT_PROBE_COOKIE_SECURE
        value: "true"
      - key: BRANCHPOINT_PROBE_ACCESS_TOKEN
        sync: false
