# Hosted proposer credentials
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional local OpenAI-compatible proposer
LOCAL_LLM_URL=http://localhost:1234/v1
CAUSALRAG_SMALL_MODEL=local-model

# Frontier proposer default shown by the Playable Probe
CAUSALRAG_FRONTIER_MODEL=gpt-5.6-terra

# Optional retrieval configuration
CAUSALRAG_EMBEDDING_MODEL=text-embedding-3-small
CAUSALRAG_INDEX_PATH=
CAUSALRAG_GRAPH_PATH=

# Playable Probe / API
CAUSALRAG_API_HOST=0.0.0.0
CAUSALRAG_API_PORT=8765

# External-model access control.
# Local development can leave auth disabled. Public deployments with paid
# provider credentials should require owner access and secure cookies.
CAUSALRAG_REQUIRE_PROBE_AUTH=false
CAUSALRAG_PROBE_ACCESS_TOKEN=
CAUSALRAG_PROBE_COOKIE_SECURE=false
